# Example: first five minutes

```bash
curl -fsSL https://x.ai/cli/install.sh | bash
cd your-project
grok inspect
grok plugin install superpowers@xai-official --trust
grok inspect
grok
```

In the TUI:

```
Use inspect-and-ship. Do not edit yet.
```

If the repo is this playbook:

```
Read AGENTS.md. Propose the smallest docs fix. Wait.
```
